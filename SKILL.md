---
name: linux-performance-analysis
description: 诊断 Linux 系统在 CPU、内存、I/O 和网络方面的性能问题与瓶颈。适用于分析 Linux 主机运行缓慢、过载、卡顿、超时、丢包、内存交换、磁盘阻塞，或显示高负载/高延迟等问题。用于确定瓶颈所在的子系统、判断瓶颈在内核态还是应用态，以及定位导致问题的具体进程。
---

# Linux 性能分析

## 概述

使用此技能进行结构化的 Linux 性能诊断。始终覆盖 CPU、内存、I/O 和网络，从系统级症状逐步深入到具体进程（当瓶颈在应用层时）。

优先使用只读检查进行故障排除。如果用户只提供了命令输出，使用相同的工作流程解析该输出，而不是要求用户提供全新的数据集。

## 安全第一（低侵入默认）

默认使用最低侵入性的工作流程，特别是在生产主机上。

- L1（默认）：只读、低开销观察。
- L2（条件性）：短时间、有针对性的采样，有严格的时间限制。
- L3（升级）：仅在获得明确用户授权后，才能使用 attach、追踪、抓包、eBPF 或主动负载测试。

执行任何 L3 命令前：

1. 确认主机角色（生产或非生产）和时间窗口。
2. 一句话说明预期的开销。
3. 设置明确的持续时间或采样上限。
4. 优先选择最能减少不确定性的命令。

除非用户明确要求并接受风险，否则避免在生产环境进行主动负载生成（如 `iperf3`）。

## 工作流程

1. 确定症状和时间窗口。
2. 采集轻量级的系统级基线数据。
3. 全面检查 CPU、内存、I/O 和网络。
4. 对于任何可疑领域，从系统级深入到进程级。
5. 判断瓶颈主要在内核态还是应用态。
6. 给出明确回答三个关键问题的结论。

## 基线数据

在深入分析前先获取快速基线。尽可能复用用户已有的数据。

典型基线命令：

```bash
uname -a
uptime
top -b -n 1
vmstat 1 5
pidstat 1 3
```

根据当前信号添加相应参考文件中的命令。

## 分析规则

即使某个问题看起来很明显，也要分析四个领域。第一个可疑指标不一定是根本原因。

对于每个领域，遵循相同的模式：

1. 检查整体系统压力。
2. 确认压力是持续的还是突发性的。
3. 识别主要的进程、线程、socket 或设备。
4. 判断热点主要在内核侧还是应用侧。
5. 记录证据，而不仅仅是猜测。

注意这些常见交叉信号：

- 高 `wa` 通常指向 I/O，而非 CPU。
- 高负载但 CPU 使用率低可能意味着任务被阻塞，通常是 I/O 或锁等待。
- 高 `sy`、`si`、`hi`、重传或积压压力通常指向内核/网络处理。
- Swap、主页面错误、回收停滞或 OOM 事件指向内存压力。
- 网络症状可能由上游的 CPU 饱和、socket 积压或应用读写行为引起。

## 内核态 vs 应用态判断

使用以下经验法则分类瓶颈：

- 当主要开销发生在用户进程、用户态线程、请求处理器、GC、查询执行、序列化、压缩或其他业务/应用逻辑中时，归类为应用态。
- 当主要开销发生在调度、中断、软中断、回收、文件系统/块层、TCP/IP 栈、驱动处理、系统调用密集型行为或其他内核介导的路径中时，归类为内核态。
- 如果应用程序导致大量内核工作，同时报告两个方面：瓶颈层是内核态，触发应用程序进程是导致该内核压力的用户态进程。

## 必需参考文件

深入分析前先阅读匹配的参考文件：

- CPU：[references/cpu.md](references/cpu.md)
- 内存：[references/memory.md](references/memory.md)
- I/O：[references/io.md](references/io.md)
- 网络：[references/network.md](references/network.md)

使用 [references/report-template.md](references/report-template.md) 作为最终报告格式。

## 输出约定

始终以简洁的结论部分结尾，明确说明：

1. 主要瓶颈是 CPU、内存、I/O 还是网络。
2. 该领域的瓶颈主要在内核态还是应用态。
3. 如果涉及应用态，哪个进程是主要触发因素。

如果证据不完整，说明已确认什么、仅怀疑什么，以及下一步哪个命令最能减少不确定性。
